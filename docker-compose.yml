version: "3.9"
services:
  app:
    build: .
    container_name: bcomandas-app
    environment:
      - NODE_ENV=production
      - PORT=8080
      - VITE_API_URL=${VITE_API_URL:-http://localhost:3000}
    restart: unless-stopped
